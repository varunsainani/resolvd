import { CannedView } from "@/components/canned";

// Canned response management. Interactive list + CRUD live in CannedView (client).
export default function CannedPage() {
  return <CannedView />;
}
