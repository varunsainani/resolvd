import { CustomersView } from "@/components/customers";

// Customer directory. The interactive list lives in CustomersView (client).
export default function CustomersPage() {
  return <CustomersView />;
}
