// Extends `expect` with the jest-dom matchers (toBeInTheDocument, toHaveClass,
// toHaveStyle, ...). Safe to load for node tests too — it only augments expect.
import "@testing-library/jest-dom/vitest";
