# Origin of the Resolvd API. In local dev the Express backend runs on :8000 and
# /api is proxied to it (see next.config.ts). In production, set this to the
# deployed API URL so the same-origin /api proxy points at it.
BACKEND_URL=http://localhost:8000
